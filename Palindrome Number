class Solution(object): 
    def isPalindrome(self, x):
        y = ""
        for i in str(x):
            y = i + y 
        if y == str(x):
            return True
        else:
            return False
