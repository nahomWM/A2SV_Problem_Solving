class Solution(object):
    def minOperations(self, logs):
        stack = []
        for a in logs:
            if a == "../" and stack:
                stack.pop()
            elif a not in ("../", "./"):
                stack.append(a)
        return len(stack)
