class Solution:
    def pivotIndex(self, nums: list[int]) -> int:
        sums = sum(nums)
        l = 0
        for i, num in enumerate(nums):
            if l == sums - l - num:
                return i
            l += num   
        return -1
