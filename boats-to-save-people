class Solution:
    def numRescueBoats(self, people, limit):
        people.sort()
        l, r = 0, len(people) - 1
        sol = 0
        while l <= r:
            if people[l] + people[r] <= limit:
                l += 1
            r -= 1
            sol += 1
        return sol
