class Solution(object):
    def judgeSquareSum(self, c):
        squared = set()
        i = 0
        while i * i <= c:
            squared.add(i * i)
            i += 1
        for x in squared:
            if (c - x) in squared:
                return True
        return False
