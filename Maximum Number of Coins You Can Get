class Solution:
    def maxCoins(self, piles):
        piles.sort()
        n = len(piles) // 3
        sol = 0
        j = len(piles) - 2   
        for i in range(n):
            sol += piles[j]
            j -= 2
        
        return sol
        
