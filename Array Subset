from collections import Counter
class Solution:
    def isSubset(self, a, b):
        count_a = Counter(a)
        for element in b:
            if count_a[element] == 0:
                return False
            count_a[element] -= 1
        return True
