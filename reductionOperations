class Solution(object):
    def reductionOperations(self, nums):
        nums.sort()
        main = 0
        a = 0       
        for i in range(1, len(nums)):
            if nums[i] != nums[i - 1]:
                a += 1
            main += a    
        return main
