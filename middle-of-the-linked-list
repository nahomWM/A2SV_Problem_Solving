class Solution:
        def middleNode(self, head):
            arr = [] 
            while head:
                arr.append(head)
                head = head.next  
            return arr[len(arr)//2]
